{
  "name": "root",
  "private": true,
  "devDependencies": {
    "commitizen": "^3.0.7",
    "cz-lerna-changelog": "^2.0.2",
    "lerna": "^3.13.1",
    "lerna-semantic-release": "^9.1.0"
  },
  "dependencies": {
    "npm": "^6.9.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-lerna-changelog"
    }
  },
  "scripts": {
    "commit": "npx git-cz",
    "git-cz": "npx git-cz",
    "semantic-release": "npx lerna-semantic-release pre && npx lerna-semantic-release perform && npx lerna-semantic-release post"
  }
}
